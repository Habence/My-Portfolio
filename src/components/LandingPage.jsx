import React, { useEffect, useRef } from "react";
import {
  motion,
  useInView,
  useAnimation,
  useTransform,
  useScroll,
} from "framer-motion";

import "../index.css";

const LandingPage = () => {
  return <div></div>;
};

export default LandingPage;
